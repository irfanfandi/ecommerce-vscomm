import LoginForm from "@/components/LoginForm";
import { Fragment } from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default page;
